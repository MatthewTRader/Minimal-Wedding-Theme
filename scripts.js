	$(window).load(function(){ 
	  $('#slider').show().bxSlider({
	    startSlide:0,
	    pager: false,
	    auto: true,
	    mode:'horizontal',
	    infiniteLoop: true,
	    touchEnabled: true,
	    controls: true,
	    autoHover: true,
	    speed:700,
	    pause:3000,
	    preloadImages: 'all'
	    });
	});
                            